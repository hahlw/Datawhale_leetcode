class Solution {
    public boolean containsDuplicate(int[] nums) {
        HashSet setNum = new HashSet();
        int len =nums.length;
        for (int i=0;i<len;i++){
            if (setNum.add(nums[i])==false){
                return true;
            }
        }
        return false; 
        
    }
}
